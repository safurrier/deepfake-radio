deployments:
- name: DeepfakeRadio
  type: code
  engine: python
  config:
    build:
      commands:
        - "pip install -r requirements.txt" # Install dependencies
        - "python main.py" # Run the bot
